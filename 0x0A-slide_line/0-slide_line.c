#include "slide_line.h"
#include <string.h>

/**
 * slide_line - function that checks whether or not a given unsigned
 * integer is a palindrome.
 * @line: unsigned number to evaluate
 * @size: unsigned number to evaluate
 * @direction: unsigned number to evaluate
 *
 * Return: 1 if n is a palindrome, and 0 otherwise
 */

int slide_line(int *line, size_t size, int direction)
{
	return (1);
}

